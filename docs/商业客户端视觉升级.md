# 商业客户端视觉升级

深墨蓝背景、香槟金主操作、青绿连接状态。页头、工作区导航、服务器侧栏与主内容区域形成稳定布局。开始对局和牌组工坊分开呈现，切换保留未保存编辑。原场景作为低对比横幅装饰。

实施：ArenaLobby.tsx 增加工作区切换和当前牌组摘要；commercialUi.css 统一颜色、间距、焦点、面板与响应式布局。卡牌比例、规则状态、连接和存储仍使用现有实现。

验收：运行 pnpm test 和 pnpm build；浏览器检查工作区切换、编辑保留、连接、桌面与窄屏布局。连接状态来自真实服务，不展示虚构在线人数。源码验证后提交，不发布新安装包。

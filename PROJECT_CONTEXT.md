# MTG Tabletop 快速上下文

这份文档给新接手项目的人或 Codex 使用，用来在几分钟内理解项目现状。完整安装说明、功能说明和用户向文档仍以 `README.md` 为准。

---

## 一句话定位

MTG Tabletop 是一个面向朋友娱乐对局的《万智牌》双人网页牌桌。

它更像 Tabletop Simulator / 线下牌垫的轻量数字版，而不是 MTG Arena 或完整规则引擎。项目目标是降低远程摩登对战的操作成本：同步牌桌、手牌、牌库、坟场、放逐、堆叠、备牌、Token、骰子、阶段与记录。

核心原则：

- 不自动判断费用、优先权、合法目标、状态动作或复杂裁定。
- 玩家自行判断规则合法性。
- 工具只负责“牌桌状态同步”和“让常见实体操作更顺手”。

---

## 当前技术栈

| 层 | 技术 |
| --- | --- |
| 前端 | React + Vite + TypeScript |
| 服务端 | Express + WebSocket `ws` |
| 数据同步 | 单房间内通过 WebSocket 广播完整房间视图 |
| 卡图 | 前端入房前调用 Scryfall，服务端把图片字段写入牌对象 |
| 远程联机 | 本地服务 + Cloudflare Quick Tunnel |
| 桌面启动 | Windows `.cmd` / PowerShell 脚本，不是 Electron App |

默认端口：

- 开发前端：`http://127.0.0.1:5180`
- 服务端 / 生产访问：`http://127.0.0.1:8787`

---

## 常用命令

```powershell
cd D:\MTG-Tabletop
pnpm install
pnpm dev
pnpm build
pnpm start
```

Windows 脚本：

```powershell
.\scripts\dev.cmd
.\scripts\build.cmd
.\scripts\remote.cmd
.\scripts\create-desktop-shortcut.cmd
```

`create-desktop-shortcut.cmd` 会创建桌面快捷方式。之后双击 `MTG Tabletop`，会自动构建、启动本地服务、打开浏览器、启动 Cloudflare Tunnel，并尽量复制公网链接。

---

## 目录速览

```txt
D:\MTG-Tabletop
├─ src
│  ├─ client
│  │  ├─ App.tsx        # 主要前端界面与交互逻辑
│  │  ├─ i18n.ts        # 中英界面与部分日志翻译
│  │  ├─ main.tsx       # React 入口
│  │  └─ styles.css     # 全局 UI 样式
│  ├─ server
│  │  └─ index.ts       # Express/WebSocket 房间状态服务
│  └─ shared
│     └─ types.ts       # 前后端共享类型与消息协议
├─ scripts              # Windows 启动、构建、远程、桌面快捷方式脚本
├─ public
│  └─ mtg-card-back.png # 牌背图
├─ dist                 # 构建产物，不手改
├─ tools                # cloudflared.exe 等本地工具，不提交 exe
├─ README.md            # 面向用户的完整说明
└─ PROJECT_CONTEXT.md   # 本文件
```

---

## 关键运行模型

### 1. 房间状态

服务端维护内存房间：

- 玩家
- 牌库 / 手牌 / 看顶牌 / 备牌
- 公共区域：战场、坟场、放逐、堆叠
- 阶段、当前玩家、日志
- WebSocket 客户端连接

服务端不会持久化到数据库。重启服务端后房间消失。

### 2. 重连

前端会把最近房间号保存在浏览器本地。刷新或 WebSocket 意外断开后，前端会尝试用：

- 本地玩家 ID
- 最近房间号

重新绑定座位。重连不会重新导入牌表，也不会洗牌或清空战场。

主动点击“退出房间”会清除本地房间记忆。

### 3. 私密信息

服务端根据观看者生成房间视图：

- 自己能看到自己的手牌、牌库、看顶牌、备牌。
- 对手看不到这些区域的真实内容。
- 普通盖放牌对对手隐藏真实牌名和卡图。
- 双面牌翻面在战场上按公开信息显示背面卡图。

手牌、看顶、牌库、备牌里的盖放 / 翻开只进入私密记录；公开区里的盖放 / 翻开才进入公开记录。

---

## 主要功能现状

### 对局与区域

- 创建 / 加入双人房间。
- 入房前必须导入有效牌表。
- 入房后牌表锁定，只能换备。
- 牌库自动洗牌、抓牌、调度、找牌、看顶。
- 手牌可拖动排序。
- 手牌可拖到 CAST 区进入堆叠。
- 场上牌可拖到另一张场上牌上，模拟佩戴 / 灵气 / 依附。
- 手牌也可直接拖到场上牌上依附。
- Token 支持自定义卡名、可选攻防、移出游戏。

### 堆叠

- 实体牌进入堆叠后可选择进场、进坟、放逐。
- 场上触发式 / 启动式异能进入堆叠时，本体不离场。
- 堆叠异能只能“处理”，不能当作实体牌移到战场、坟场或放逐。

### 看顶 / Manifest

- 看顶会把牌从牌库顶抽出到自己的临时处理视图。
- 处理一张就减少一张。
- 看顶记录为私密。
- 看顶牌可以盖放 / 翻开，且不公开，便于模拟 Manifest 等效果。
- 看顶牌放回牌库时，公开记录不显示具体是哪张牌。

### 多选

坟场、放逐、看顶弹窗支持多选模式：

- 默认单选操作。
- 点击“多选”后可以选择复数张。
- 单选和全选使用同一套勾选形式。
- 下方操作会统一作用于所有已选牌。

### 阶段

有手动和自动两种模式：

- 手动模式：按完整阶段列表推进。
- 自动模式：只保留常用大阶段：
  - 战斗前主要阶段
  - 战斗阶段
  - 第二个主要阶段
  - 结束阶段

自动模式下，结束回合会：

1. 切换到对手。
2. 自动重置对手战场。
3. 自动抽 1。
4. 进入对手战斗前主要阶段。

---

## 远程联机模式

当前不打包 Electron App。推荐方式是本地 Web 服务 + Cloudflare Quick Tunnel。

优点：

- 对手不需要安装 App，浏览器打开链接即可。
- 修改功能后无需重新分发安装包。
- 调试简单。

常用方式：

```powershell
.\scripts\remote.cmd
```

或桌面快捷方式：

```powershell
.\scripts\create-desktop-shortcut.cmd
```

Cloudflare Quick Tunnel 是临时链接：

- 每次地址可能不同。
- 窗口关闭后链接失效。
- 没有生产级在线保证。

---

## 设计取舍

### 为什么不是完整规则引擎？

完整万智牌规则引擎工程量极大，需要处理优先权、替代效应、持续效应、层、状态动作、触发、目标合法性、费用支付、复制、牌张 Oracle 文本等。这个项目目前定位为娱乐和朋友远程对局，规则判断交给玩家。

### 为什么不是 Electron App？

当前阶段网页形态更合适：

- 对手无需安装。
- 更新快。
- tunnel 与网页服务天然匹配。
- 打包 App 会增加体积、分发和杀软拦截风险。

目前用桌面启动脚本解决“双击启动”的体验问题。

### 为什么强调私密 / 公开记录分离？

摩登对局中很多操作涉及隐藏信息，例如手牌、牌库顶、Manifest、找牌、换备。记录系统必须避免意外暴露隐藏信息。新增功能时默认先问：这个操作是否应该公开？

---

## 常见开发注意事项

1. 改消息协议时，同时改：
   - `src/shared/types.ts`
   - `src/server/index.ts`
   - `src/client/App.tsx`

2. 新增隐藏信息相关操作时，优先在服务端判断是否公开，不要只靠前端隐藏。

3. 卡图来源是 Scryfall，避免把大量图片二进制提交到仓库。

4. `dist/`、`node_modules/`、`tools/*.exe` 不应提交。

5. Windows PowerShell 5 对无 BOM UTF-8 脚本的中文字符串可能有编码问题。桌面启动脚本内的提示文字尽量使用英文 ASCII。

6. 改 UI 后至少运行：

```powershell
pnpm build
```

7. 远程 push 可能因网络无法连接 GitHub。若本地 `main...origin/main [ahead 1]`，说明提交在本地但还没推上去。

---

## 当前已知状态

截至 2026-07-10：

- 项目主分支：`main`
- GitHub 仓库：`https://github.com/Ordoviciancn/MTG-Tabletop.git`
- Git 用户：
  - name: `Ordoviciancn`
  - email: `felix_hyc@qq.com`
- 最近曾出现 GitHub 443 连接失败，导致本地提交 ahead 远端。
- 新增桌面启动器脚本后尚未确认是否已 push。

---

## 推荐下一步

优先级建议：

1. 用真实双人远程对局测试重连、退出房间、看顶盖放、多选移动。
2. 优化桌面启动器的链接识别与失败提示。
3. 为常用复杂操作增加更少点击的快捷入口，例如：
   - 从坟场施放到堆叠
   - 从放逐施放到堆叠
   - 看顶批量回顶 / 回底排序
4. 若未来要长期公网稳定服务，再考虑固定域名 / named tunnel / 云服务器。

